<UserControl x:Class="MetricsManagerClient.ChartControls.ChartDotNet"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:MetricsManagerClient.ChartControls" 
             d:DataContext="{d:DesignInstance Type=local:ChartDotNet}"
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="500">
    <Grid Height="240" Width="500">
        <Grid>
            <lvc:CartesianChart Name="TimePowerChart" Series="{Binding LineSeriesValues}" LegendLocation="Top" DisableAnimations="True">
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Байт в heap (Gen1,Gen2)"></lvc:Axis>
                </lvc:CartesianChart.AxisY>
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Время" Labels="{Binding Labels}" LabelsRotation="-8"></lvc:Axis>
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>
        </Grid>
    </Grid>
</UserControl>
